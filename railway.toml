[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @syntech/backend build"

[deploy]
startCommand = "node apps/backend/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
